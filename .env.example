# MongoDB-anslutningssträng
# Ersätt <username>, <password>, <cluster-url>, <database-name>, och <app-name> med dina egna värden.
DATABASE=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority&appName=<app-name>